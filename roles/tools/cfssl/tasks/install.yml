---
- name: Download and install cfssl tool
  get_url:
    url: "{{ cfssl_package_url }}{{ item }}_linux-amd64"
    dest: "/usr/local/bin/{{ item }}"
    mode: '0755'
    force: yes
  loop: "{{ cfssl_packages }}"